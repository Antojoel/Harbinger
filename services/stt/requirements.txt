fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
numpy>=1.26.0

# Default engine: Whisper on CTranslate2.
faster-whisper>=1.1.0

# CUDA runtime libs for CTranslate2 (cuBLAS + cuDNN 9). Harmless on CPU-only
# hosts; the Dockerfile puts them on LD_LIBRARY_PATH.
nvidia-cublas-cu12>=12.4.0
nvidia-cudnn-cu12>=9.1.0,<10

# Fallback engine: sherpa-onnx streaming transducer (STT_ENGINE=sherpa).
sherpa-onnx>=1.10.0
huggingface-hub>=0.24.0
